# 智能知识管理平台 - 核心依赖项
# 架构：SQLite（主存储）+ Redis（可选缓存）+ AI（Claude/Qwen/Ollama）

# ==================== 核心依赖 ====================

# HTTP 客户端
aiohttp>=3.9.0
requests>=2.31.0

# 数据解析
feedparser>=6.0.10
beautifulsoup4>=4.12.0
python-dateutil>=2.8.2

# 环境变量
python-dotenv>=1.0.0

# 类型提示支持
typing-extensions>=4.8.0

# ==================== 数据存储 ====================

# Redis 高性能缓存（可选，WSL 部署）
redis[hiredis]>=5.0.1

# SQLite 已内置在 Python 中，无需额外安装

# ==================== 数据处理 ====================

# 数据处理（可选，CSV/数据分析）
pandas>=2.0.0
numpy>=1.24.0

# ==================== AI 集成 ====================

# OpenAI 兼容客户端（用于 Qwen API 调用）
openai>=1.0.0

# ==================== GUI 依赖 ====================

# Tkinter 已内置在 Python 中，无需额外安装

# ==================== Web 自动化（可选）====================

# Selenium 浏览器自动化（用于绕过反爬虫检测）
selenium>=4.38.0

# WebDriver 管理器（可选，自动下载浏览器驱动）
# webdriver-manager>=4.0.0
