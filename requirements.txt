# 智能知识管理平台 - 核心依赖项
# 架构：SQLite（主存储）+ Redis（可选缓存）+ AI（Claude/Qwen/Ollama）
#
# 版本策略：下界=已验证可用版本（保证可复现），上界=下个大版本（防止破坏性升级漂移）。
# 锁定的精确组合见 requirements.lock（pip freeze 生成，用于完全一致的部署）。

# ==================== 核心依赖（必需） ====================

# HTTP 客户端
aiohttp>=3.13.1,<4.0.0
requests>=2.32.5,<3.0.0

# 数据解析
feedparser>=6.0.12,<7.0.0
beautifulsoup4>=4.14.2,<5.0.0
python-dateutil>=2.9.0,<3.0.0

# 环境变量
python-dotenv>=1.1.1,<2.0.0

# 类型提示支持
typing-extensions>=4.15.0,<5.0.0

# 安全配置加密（secure_config.py 必需）
cryptography>=42.0.0,<50.0.0

# 知识图谱内存图数据库（CVE × DSA × 产品 × CWE 关联分析）
networkx>=3.0,<4.0

# ==================== 数据处理（推荐） ====================

# CSV/数据分析、Excel 导出
pandas>=2.3.2,<3.0.0
numpy>=1.26.4,<2.0.0
openpyxl>=3.1.5,<4.0.0

# 图表 / 知识图谱可视化
matplotlib>=3.10.6,<4.0.0

# 日历组件（IT 新闻简报标签页的"日历·资讯存档"模块所需，缺失则退化为提示文字）
tkcalendar>=1.6.1,<2.0.0

# ==================== AI 集成（推荐） ====================

# OpenAI 兼容客户端（用于 Qwen API 调用）
openai>=2.3.0,<3.0.0

# ==================== 可选依赖 ====================

# Redis 高性能缓存（可选，WSL 部署）
redis[hiredis]>=6.4.0,<7.0.0

# Selenium 浏览器自动化（可选，用于绕过反爬虫检测）
selenium>=4.38.0,<5.0.0

# SQLite / Tkinter 已内置在 Python 中，无需额外安装

# ==================== 开发 / 测试依赖 ====================
# 完整开发工具链请安装：pip install -e ".[dev]"
pytest>=8.0.0,<9.0.0
