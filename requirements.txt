# CVE漏洞监控系统 - 核心依赖项
# 架构：SQLite + Redis + GPU加速（可选）

# ==================== 核心依赖 ====================

# HTTP 客户端
aiohttp>=3.9.0
requests>=2.31.0

# 数据解析
feedparser>=6.0.10
beautifulsoup4>=4.12.0
python-dateutil>=2.8.2

# 环境变量
python-dotenv>=1.0.0

# 类型提示支持
typing-extensions>=4.8.0

# ==================== 数据存储 ====================

# Redis 高性能缓存（主缓存层）
redis[hiredis]>=5.0.1

# SQLite 已内置在 Python 中，无需额外安装

# ==================== 数据处理 ====================

# 数据处理（可选，CSV/数据分析）
pandas>=2.0.0
numpy>=1.24.0

# ==================== GPU 加速（可选）====================

# PostgreSQL + pgvector 用于向量存储（仅 GPU 加速需要）
# 如需启用 GPU 向量搜索功能，取消注释以下行：
# psycopg2-binary>=2.9.9

# ==================== GUI 依赖 ====================

# Tkinter 已内置在 Python 中，无需额外安装
