# CVE漏洞监控系统 - 核心依赖项
# 支持GUI版本（cve_integrated_gui.py）+ GPU 加速版

# HTTP 客户端
aiohttp>=3.9.0
requests>=2.31.0

# 数据解析
feedparser>=6.0.10
beautifulsoup4>=4.12.0
python-dateutil>=2.8.2

# 数据库
redis[hiredis]>=5.0.1
psycopg2-binary>=2.9.9  # PostgreSQL + pgvector

# 环境变量
python-dotenv>=1.0.0

# 数据处理（可选，如果需要CSV处理）
pandas>=2.0.0
numpy>=1.24.0  # GPU 加速的向量运算

# 类型提示支持
typing-extensions>=4.8.0
